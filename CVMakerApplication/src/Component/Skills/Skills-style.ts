import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        paddingHorizontal: 5,
        flexDirection: 'row',
        alignItems: 'center'
    },
    title: {
        paddingHorizontal: 3
    }
})