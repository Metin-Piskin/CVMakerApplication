import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        alignItems: 'center',
        justifyContent: 'center',
        borderWidth: 4,
        borderRadius: 10,
        borderColor:'gray'
    },
    image: {
        width: 80,
        height: 80,
        borderRadius: 5,
        resizeMode: 'contain'
    }
})